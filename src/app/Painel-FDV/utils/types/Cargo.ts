export interface Cargo {
    codCargo: number;
    nome: string;
    dataCadastro: string; // ISO date string
    dataUltimaAlteracao: string; // ISO date string
    ativo: boolean;
    authority: string;
  }
  